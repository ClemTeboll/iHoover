describe("When a user click on the execute button", () => {    

    it("throw the execute function to return final x, y and direction datas", () => {
        expect(finalX).toEqual(5);
        // expect(finalY).toEqual(6);
        // expect(finalDirection).toBe('N');
    });

});